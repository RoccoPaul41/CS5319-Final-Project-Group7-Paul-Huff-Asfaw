/* File: src/pages/DashboardPage.jsx
 * ARCHITECTURE (Layered): React UI -> api service -> monolith service chain.
 * ARCHITECTURE (Event-Driven): same UI contract; write calls trigger domain events.
 * Service communication: Context + API + WebSocket.
 * Events/Methods: REST methods in layered, event publication/consumption in EDA.
 */
import { useEffect,useMemo,useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { getDocuments,getNotifications,deleteDocument } from '../services/api'
import { useAuth } from '../contexts/AuthContext'
import Navbar from '../components/Navbar'
import Avatar from '../components/Avatar'
import Badge from '../components/Badge'
import CreateDocumentModal from '../components/CreateDocumentModal'
import ConfirmDialog from '../components/ConfirmDialog'
const rel=(iso)=>{const m=Math.floor((Date.now()-new Date(iso).getTime())/60000); if(m<60)return`${m} min ago`; if(m<1440)return`${Math.floor(m/60)} hr ago`; return'Yesterday'}
export default function DashboardPage(){const [documents,setDocuments]=useState([]); const [filter,setFilter]=useState('all'); const [sort,setSort]=useState('date'); const [notifications,setNotifications]=useState([]); const [showNew,setShowNew]=useState(false); const [toDelete,setToDelete]=useState(null); const {user,logout}=useAuth(); const navigate=useNavigate(); const load=async()=>{const [d,n]=await Promise.all([getDocuments(),getNotifications()]); setDocuments(d.data||[]); setNotifications((n.data||[]).slice(0,3))}; useEffect(()=>{load()},[]); const view=useMemo(()=>{let l=[...documents]; if(filter==='mine')l=l.filter((d)=>d.ownerId===user?.id); if(filter==='shared')l=l.filter((d)=>d.ownerId!==user?.id); if(sort==='date')l.sort((a,b)=>new Date(b.updatedAt)-new Date(a.updatedAt)); if(sort==='nameaz')l.sort((a,b)=>a.title.localeCompare(b.title)); if(sort==='nameza')l.sort((a,b)=>b.title.localeCompare(a.title)); return l},[documents,filter,sort,user]); const remove=async()=>{if(!toDelete)return; await deleteDocument(toDelete.id); setToDelete(null); load()}; return <div><Navbar currentPage='dashboard' username={user?.username} onLogout={logout} notifCount={notifications.filter((n)=>!n.read).length}/><div className='mx-auto flex max-w-app'><aside className='w-sidebar border-r border-border bg-white p-4'><p className='text-xs font-semibold text-text-secondary'>FILTER</p><div className='mt-2 space-y-1'>{[{k:'all',l:'All Documents'},{k:'mine',l:'My Documents'},{k:'shared',l:'Shared With Me'}].map((f)=><button key={f.k} className={`block w-full rounded-button px-3 py-2 text-left text-sm ${filter===f.k?'bg-primary text-white':'hover:bg-background'}`} onClick={()=>setFilter(f.k)}>{f.l}</button>)}</div><p className='mt-6 text-xs font-semibold text-text-secondary'>SORT BY</p><div className='mt-2 space-y-2 text-sm'>{[{k:'date',l:'Date Modified'},{k:'nameaz',l:'Name A→Z'},{k:'nameza',l:'Name Z→A'}].map((s)=><label key={s.k} className='block'><input type='radio' className='mr-2' checked={sort===s.k} onChange={()=>setSort(s.k)}/>{s.l}</label>)}</div><div className='mt-6 rounded-button border-l-4 border-warning bg-amber-50 p-3'><p className='text-sm font-semibold'>Notifications ({notifications.filter((n)=>!n.read).length})</p><ul className='mt-2 list-disc pl-4 text-[13px] text-text-secondary'>{notifications.map((n)=><li key={n.id}>{n.actor} {n.type} {n.documentTitle}</li>)}</ul><button className='mt-2 text-sm text-primary' onClick={()=>navigate('/notifications')}>View all →</button></div></aside><main className='flex-1 p-6'><div className='mb-4 flex items-start justify-between'><div><h1 className='text-2xl font-bold'>My Documents</h1><p className='text-[13px] text-text-secondary'>{view.length} documents · last updated {view[0]?rel(view[0].updatedAt):'n/a'}</p></div><button className='h-[38px] rounded-button bg-primary px-4 text-sm font-semibold text-white' onClick={()=>setShowNew(true)}>+ New Document</button></div><div className='grid grid-cols-[repeat(auto-fill,minmax(220px,1fr))] gap-4'><button className='flex min-h-[170px] flex-col items-center justify-center rounded-card border-2 border-dashed border-border bg-white text-text-secondary' onClick={()=>setShowNew(true)}><span className='text-2xl'>+</span><span className='text-sm'>New Document</span></button>{view.map((doc)=><article key={doc.id} className='rounded-card border border-border bg-white p-3'><h3 className='truncate text-base font-semibold'>{doc.title}</h3><p className='mt-1 text-[13px] text-text-secondary'>Owner: {doc.ownerName}</p><div className='mt-2 flex -space-x-2'>{(doc.collaborators||[]).slice(0,3).map((c)=><Avatar key={c.id} name={c.username}/>)}</div><p className='mt-2 text-xs text-muted'>Modified {rel(doc.updatedAt)}</p><div className='mt-3 flex items-center justify-between'><Badge type={doc.ownerId===user?.id?'owner':'shared'}/><div className='flex gap-1'><button className='h-7 rounded-button border border-border px-2 text-xs' onClick={()=>navigate(`/documents/${doc.id}`)}>Open</button>{doc.ownerId===user?.id?<button className='h-7 rounded-button border border-danger px-2 text-xs text-danger' onClick={()=>setToDelete(doc)}>Del</button>:null}</div></div></article>)}</div></main></div><CreateDocumentModal isOpen={showNew} onClose={()=>setShowNew(false)} onCreated={load}/><ConfirmDialog isOpen={!!toDelete} title='Delete document' message={`Delete '${toDelete?.title||''}'?`} confirmLabel='Delete' danger onCancel={()=>setToDelete(null)} onConfirm={remove}/></div> }
